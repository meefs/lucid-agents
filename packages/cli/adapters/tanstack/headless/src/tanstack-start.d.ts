// Load TanStack Start's server-route declaration merge before Vite regenerates
// the route tree. Fresh projects type-check before their first Vite build.
import type {} from '@tanstack/react-start';
