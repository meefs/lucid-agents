---
title: Release channels
description: Match documentation to public npm packages, this repository, or a separately operated product.
icon: GitBranch
status: stable
verifiedVersion: 2.5.0
verifiedAt: 2026-07-21
product: sdk
---

Lucid packages use independent versions. “Version 3” of core does not imply
that every extension or adapter is version 3, and a version in this repository
does not prove that npm contains it.

## Status contract

| Badge              | Contract                                                                            |
| ------------------ | ----------------------------------------------------------------------------------- |
| **Stable**         | Publicly installable npm surface verified from a clean external project.            |
| **Next**           | Current repository surface. It may be ahead of npm or not published.                |
| **Experimental**   | Implemented, but the API or support contract may change.                            |
| **Deprecated**     | Historical migration context; do not use it for new projects.                       |
| **Hosted product** | Separately operated service with its own availability, authentication, and support. |

The badge belongs to a page, not a package name. A Stable tutorial names and
tests exact public versions. A package reference marked Next describes the
source and manifest in this checkout.

## Package release table

Registry versions were read from npm on 21 July 2026. “Not published” means the
package name did not resolve in the public registry on that date.

| Package                   |    Public npm | This repository | How to read its docs                                                   |
| ------------------------- | ------------: | --------------: | ---------------------------------------------------------------------- |
| `@lucid-agents/a2a`       |       `0.6.2` |         `0.6.3` | Package page is Next; protocol coverage is partial and version-scoped. |
| `@lucid-agents/analytics` |       `0.3.2` |         `0.3.3` | Package page is Next.                                                  |
| `@lucid-agents/ap2`       |       `0.4.2` |         `0.4.3` | Package page is Next; role metadata only.                              |
| `@lucid-agents/api-sdk`   |       `2.5.0` |         `3.0.0` | Client for the separately operated Hosted Lucid Platform.              |
| `@lucid-agents/catalog`   | Not published |         `0.2.0` | Repository-only Next package.                                          |
| `@lucid-agents/cli`       |       `2.5.0` |         `3.0.0` | Stable quickstarts pin `2.5.0`; package page is Next.                  |
| `@lucid-agents/core`      |       `2.5.0` |         `3.0.0` | Stable quickstarts pin `2.5.0`; package page is Next.                  |
| `@lucid-agents/express`   |       `0.5.6` |         `0.6.0` | Package page is Next.                                                  |
| `@lucid-agents/hono`      |       `0.9.6` |        `0.10.0` | Stable quickstart pins `0.9.6`; package page is Next.                  |
| `@lucid-agents/http`      |      `1.10.2` |        `1.11.0` | Stable quickstart pins `1.10.2`; package page is Next.                 |
| `@lucid-agents/identity`  |       `2.5.0` |         `3.0.0` | Package page is Next.                                                  |
| `@lucid-agents/mpp`       | Not published |         `0.2.0` | Repository-only Next package.                                          |
| `@lucid-agents/payments`  |       `2.5.0` |         `3.0.0` | Stable quickstart pins `2.5.0`; package page is Next.                  |
| `@lucid-agents/scheduler` |       `0.2.2` |         `0.2.3` | Package page is Next.                                                  |
| `@lucid-agents/tanstack`  |       `0.7.4` |         `0.8.0` | Package page is Next.                                                  |
| `@lucid-agents/types`     |       `1.7.0` |         `1.8.0` | Package page is Next.                                                  |
| `@lucid-agents/wallet`    |       `0.6.2` |         `0.6.3` | Package page is Next.                                                  |

The installable Stable seller loop is narrower than the full table. It pins
CLI, core, HTTP, payments, Hono, and direct x402 buyer dependencies in
[Install Lucid](/docs/start/install), then runs the paid transaction in a clean
temporary project.

## Do not assemble a channel package by package

Avoid this pattern:

```text
latest core + repository payments + an older adapter + copied Next example
```

Extension types and runtime contracts move together. Use one generated Stable
set with a lockfile, or clone this repository and use its workspace and lockfile
as the Next set. When a package must be upgraded independently, run its type
checks plus the cross-package examples and HTTP/payment smoke tests before
shipping.

## Verify before copying an example

Check a public version:

```bash
npm view @lucid-agents/core version
npm view @lucid-agents/payments version
```

Check the current repository set:

```bash
bun install --frozen-lockfile
bun run build:packages
bun run type-check
bun run test:docs
```

If a page's badge and the package you installed disagree, stop and select one
channel. Do not silence the resulting type error with a cast.
